{
  "name": "singbox-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that turns a subscription into managed sing-box configs and BlackMatrix7-backed rule-sets.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "node --check src/index.js",
    "test": "node tests-smoke.js"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
