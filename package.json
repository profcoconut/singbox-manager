{
  "name": "singbox-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Lazy rules for sing-box: keep your existing subscription and add managed routing on top.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "node --check src/index.js",
    "test": "node tests-smoke.js"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
